#!/bin/sh
# The Slack workspace as an MCP server. Harbor registers this from task.toml,
# so the agent's harness discovers the tools; nothing in the prompt has to name
# them. The workspace itself runs in another container -- this is a client.
exec python3 -m slack_sim.mcp_server "$@"
