#!/bin/sh
# The Gmail environment as an MCP server. Harbor registers this from task.toml,
# so the agent's harness discovers the tools; nothing in the prompt has to name
# them. The environment itself runs in another container -- this is a client.
exec python3 -m gmail_sim.mcp_server "$@"
