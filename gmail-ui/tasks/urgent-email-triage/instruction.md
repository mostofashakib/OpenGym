You are tasked with urgent email triage and follow-up in this mailbox:

1. **Review and Flag Invoice**:
   - Locate the invoice or billing email from `billing@company.com` or `finance@corp.co` (or with subject containing `Invoice`).
   - Mark this message as starred (`isStarred: true`), important (`isImportant: true`), and read (`isRead: true`).

2. **Archive System Notices**:
   - Locate any notification or shipping notices (such as `Shipping notice` or `Your order confirmation`).
   - Archive them (`isArchived: true`) so they are filed away properly.

3. **Prepare Confirmation Draft**:
   - Compose and save a new draft email to `finance@corp.co` with subject `Payment Confirmation Received` and body `Thank you, I have reviewed the invoice and marked it for processing.`

4. **Submit Report**:
   - When finished, submit your final report using `submit_task` with a summary of the messages triaged.
