Jordan Kim (U004) is stepping back from active sprint work, so their queue needs
to move before the next planning round.

Reassign every task currently assigned to Jordan:

- a task that belongs to a milestone goes to Morgan Patel (U002);
- a task with no milestone goes to Riley Stone (U003), and gets `needs-triage`
  added to the labels it already has.

Leave everything else in the tracker exactly as you found it.

When you are done, record what you changed with `submit_handover_report`: the
ids of the tasks you modified, and a one-line summary.
