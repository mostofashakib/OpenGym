[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "software-ui"
version = "1.0.0"
description = "Procedural SaaS Console UI environment and Harbor evaluation workspace"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0.0",
    "mcp>=1.0.0",
]

[tool.setuptools]
packages = ["software_sim", "tools", "verifiers", "solution"]

[tool.setuptools.package-dir]
software_sim = "environment/software_sim"

[tool.pytest.ini_options]
pythonpath = ["environment", ".", "tests"]
testpaths = ["tests"]
